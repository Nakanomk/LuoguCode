#include <bits/stdc++.h>
using namespace std;
int main()
{
	
	// int n; cin >> n;
	// int ca = 0, cb = 0, cc = 0;
	// for(int i = 0; i < n; i++) {
	// int a, e; bool c;
	// cin >> a >> e >> c;
	// ca += 8 - a; cb += 12 - e; cc += 1 - c;
	// }
	// ca /= 8; cb /= 12;
	// int re = min(ca, cb);
	// re = min(re, cc);
	// cout << re;

	system("pause");
	return 0;
}